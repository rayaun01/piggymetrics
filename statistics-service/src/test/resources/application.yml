hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000

spring:
  data:
    mongodb:
      database: piggymetrics
      port: 0

rates:
  url: ${RATES_URL:http://localhost:1}