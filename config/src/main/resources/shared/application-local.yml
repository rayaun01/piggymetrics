eureka:
  client:
    serviceUrl:
      defaultZone: http://${REGISTRY_HOST:localhost}:8761/eureka/

security:
  oauth2:
    resource:
      user-info-uri: http://${AUTH_HOST:localhost}:5000/uaa/users/current

spring:
  cloud:
    bus:
      enabled: false
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
